server:
  port: 8080

spring:
  datasource:
    username: root
    password: 20041114Liuyu!
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL:jdbc:mysql://localhost:3306/WorkManagement?useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true}

  jpa:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

logging:
  level:
    root: info
    org.hibernate.SQL: debug

wechat:
  appid: ${WECHAT_APPID:}
  secret: ${WECHAT_SECRET:}
