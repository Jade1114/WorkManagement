# 前端课设开发说明

> 目标：将现有前端提炼为独立的 Vue 后台管理课设，覆盖顶部区、左侧导航、右侧内容区与扩展模块，形成完整可演示的后台系统。

## 1. 整体架构
- 技术栈：Vue 3 + Vite + Pinia + Vue Router + Element Plus。
- 入口：`src/main.js`，布局统一由新建的 `AdminLayout` 提供（顶部 + 侧边栏 + 内容槽位）。
- 路由：登录/注册保持独立；其余业务路由挂在 `AdminLayout` 下，根据角色（admin/teacher/student）过滤菜单。
- 角色与职责（依据 `logic.md`）：
  - Admin：可修改用户/角色，学科 CRUD，同步具备教师发布/批改能力。
  - Teacher：学科 CRUD，发布作业、查看/批改提交。
  - Student：查看作业、提交作业、查看已提交。

## 2. 导航与布局
- 顶部：系统名称、当前用户信息、登录状态/退出、快捷入口（如通知/帮助）。
- 左侧导航（示例分组）：
  - 数据大屏（可为占位/外链）
  - 仪表盘概览
  - 用户管理（Admin 可见，含角色调整）
  - 学科管理（Admin/Teacher）
  - 作业管理（发布、批改）（Admin/Teacher）
  - 学生列表（Teacher 辅助视图）
  - 学生端：待提交/已提交作业（Student）
- 右侧内容：对应子路由的组件（统计、表格、表单、上传占位）。

## 3. 计划新增/改造页面
- `DashboardView`：用户/作业/学科统计卡片 + 最近提交/作业表格。
- `UserManageView`（Admin）：用户查询/角色调整/状态切换（占位数据 + 状态开关）。
- `CourseManageView`：学科列表 + 新建/编辑/删除（沿用 `/courses/*` 接口）。
- `AssignmentManageView`：发布/批改双 tab（复用现有 Teacher 作业页，适配 Admin）。
- `StudentListView`：学生列表（辅助）。
- `DataScreenView`：大屏页面或外链占位。
- 学生端页面：待提交、已提交（沿用现有接口）。

## 4. 数据与接口策略
- 已有接口：继续使用 `/teacher/stats`、`/assignments/*`、`/submissions/*`、`/courses/*` 等。
- 新增模块（如用户管理、数据大屏）：先用本地 Mock 数据/前端状态占位，预留接口函数（集中在 `src/net` 或对应视图的 `loadX` 方法）。
- 角色判断：基于 `userStore.role`，菜单和路由守卫按 admin/teacher/student 过滤；未知角色落地错误页。

## 5. UI 规范
- 主题与变量：在 `src/style.css` 扩充色板、阴影、间距，保持卡片+栅格+表格风格。
- 布局：固定顶部，高度可滚动；侧栏可折叠；内容区留边距与卡片容器。
- 自适应：侧栏在窄屏折叠为抽屉；卡片栅格 `auto-fit minmax`。

## 6. 迭代步骤（建议顺序）
1) 创建 `AdminLayout`（顶部/侧栏/内容插槽），接管主业务路由。
2) 改造路由表：挂载新页面，占位数据；登录/注册保持独立。
3) 新增各模块视图文件（Mock 数据 + 表单/表格/卡片/上传占位）。
4) 补充全局样式与布局样式；侧栏菜单按角色过滤。
5) 快速自测：路由跳转、菜单激活态、表格/弹窗交互、移动端折叠。

## 7. 提交与演示要点
- 界面展示：顶部区信息、左侧导航、右侧功能区的完整性。
- 功能演示：至少演示统计卡片刷新、表格增删改占位、上传/替换占位、大屏入口。
- 代码结构：组件化、路由分层、状态管理清晰，便于答辩讲解。 
